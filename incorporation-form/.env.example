# Copy to .env before running `npm run build` / `npm run deploy`. Must match the
# nexdash-worker's SYNC_API_KEY secret exactly (see nexdash-worker/README.md).
# Never commit the real value — this repo is public.
VITE_SYNC_API_KEY=
